def _subtract_two_numbers(a: float, b: float) -> float:
    """Use this tool to subtract two numbers and return the result."""
    return a - b

_subtract_two_numbers._meta = {"requires_approval": True}